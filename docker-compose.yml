services:
  postgres:
    image: postgres:16
    container_name: trouble-ticket-db

    environment:
      POSTGRES_DB: trouble_ticket
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5433:5432"